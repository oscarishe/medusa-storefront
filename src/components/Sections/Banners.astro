---
import { IBanner } from "../../types/types";
import Banner from "../Banner/Banner.astro";
interface Props {
    banners: IBanner[];
}

const { banners } = Astro.props;
---

<section class="banners">
    <div class="container">
        <div class="row">
            <!-- <div class="col">
                <Banner link="#" imgSrc="/assets/images/banners/banner1.png" />
            </div>
            <div class="col">
                <Banner link="#" imgSrc="/assets/images/banners/banner1.png" />
            </div> -->
            {
                banners.map((item) => (
                    <div class="col-md mb-3">
                        <Banner link={item.link} imgSrc={item.imgSrc} />
                    </div>
                ))
            }
        </div>
    </div>
</section>
